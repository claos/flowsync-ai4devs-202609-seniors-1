{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */frontend/*|frontend/*) npx --yes prettier --ignore-unknown --write \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "Formateando frontend con Prettier..."
          }
        ]
      }
    ]
  }
}
